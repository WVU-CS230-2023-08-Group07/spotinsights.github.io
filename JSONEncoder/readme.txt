Requires installation of Node.js to run this JavaScript file
Once Node.js is installed, use the Windows command prompt to navigate to this project directory (...\spotify-analytics\accountsJS)
When in the project directory, install the project dependencies from package.json using the command "npm install"
When in the project directory, you can run the JavaScript file using Node.js with the command "node jsonConverter.js"